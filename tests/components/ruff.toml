# Extends the repository's Ruff rules for the tests, mirroring the
# tests/ruff.toml of home-assistant/core.
extend = "../../pyproject.toml"

[lint]
extend-ignore = [
  "B904",   # Use raise from to specify exception cause
  "N806",   # Variable {name} in function should be snake_case
  "N815",   # Variable {name} in class scope should not be mixedCase
  "RUF018", # Avoid assignment expressions in assert statements
  "DTZ011", # date.today() used: tests commonly use naive dates
  "PLR0913", # Too many arguments: a parametrized test takes one per case column
  "S101",   # Use of assert detected: that is what tests are made of
  "S107",   # Possible hardcoded password assigned to function default
  "SLF001", # Private member accessed: tests do test internals
]

[lint.flake8-tidy-imports.banned-api]
"async_timeout".msg = "use asyncio.timeout instead"
"pytz".msg = "use zoneinfo instead"
"syrupy.SnapshotAssertion".msg = "use syrupy.assertion.SnapshotAssertion instead"

[lint.isort]
known-first-party = ["homeassistant"]
known-third-party = ["pytest", "pylint", "syrupy", "voluptuous"]
forced-separate = ["tests"]
